apply plugin: 'com.android.application'

android {
  compileSdkVersion 24
  buildToolsVersion '24.0.3'

  defaultConfig {
    applicationId 'com.sqisland.android.hello'
    minSdkVersion 21    // must be over 9 to add basic activity
    targetSdkVersion 24
    versionCode 1
    versionName '1.0.0'
  }
}